@page "/auth"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<PageTitle>Auth</PageTitle>

<h1>You are authenticated</h1>

<AuthorizeView>
    Hello @context.User.Identity?.Name!
    twoje id @context.User.Identity.IsAuthenticated!
    id @context.User.
</AuthorizeView>


@code{

}